# Vimの小技メモ🐶

## タグの中身だけを一気に書き換える

- `cit`(Change Inner Tag)

  タグの中にカーソルがある状態で`cit`を打つと、中身が消えて新しいコードが書ける。

  (挿入モードになる)

- `dit`(Delete Inner Tag)

  タグの中身を消すだけならこれでok。

その他にも`ci"`や`ci'`など応用可能🐶

## 対応するタグへジャンプ

ネストが深いときなど、この`<div>`の対応はどこか迷子になるやつ。

開始タグの上で、`%`を押すと対応するペアにジャンプしてくれる。

